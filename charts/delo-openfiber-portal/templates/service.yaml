apiVersion: v1
kind: Service
metadata:
  name: {{ include "delo-openfiber-portal.fullname" . }}
  labels:
    {{- include "delo-openfiber-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "delo-openfiber-portal.selectorLabels" . | nindent 4 }}
